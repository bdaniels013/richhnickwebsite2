{
  "name": "richhnick-org",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public/assets && curl -sL https://github.com/tailwindlabs/tailwindcss/releases/latest/download/tailwindcss-linux-x64 -o tailwindcss && chmod +x tailwindcss && ./tailwindcss -i ./src/input.css -o ./public/assets/tailwind.css --minify --content ./public/index.html"
  }
}
